#Область СлужебныйПрограммныйИнтерфейс

// Регистрирует сценарии тестового модуля.
	Процедура ИсполняемыеСценарии() Экспорт
		ЮТТесты
					.ДобавитьТест("НовыйИнструментЗаполняетРежимВыполнения")
					.ДобавитьТест("НовыйИнструментСОтложеннымОтветомЗаполняетПоля")
					.ДобавитьТест("НовыйИнструментСНекорректнымОтложеннымОтветомВызываетИсключение")
				.ДобавитьТест("НовыйРесурсЗаполняетMimeType")
			.ДобавитьТест("НовыйШаблонРесурсаЗаполняетMimeType")
			.ДобавитьТест("НовыйПромптЗаполняетПоля")
			.ДобавитьТест("НовыйПромптСНекорректнымиАргументамиВызываетИсключение")
			.ДобавитьТест("ПараметрыСервераФормируютКонтракт")
			.ДобавитьТест("ПараметрыСервераПоУмолчаниюФиксируютТаймаут900Секунд")
			.ДобавитьТест("ПараметрЗапускаБезКомандыНеФормируетПараметрыСервера")
			.ДобавитьТест("ПараметрЗапускаRunMcpИспользуетПорт8080")
			.ДобавитьТест("ПараметрЗапускаRunMcpРегистронезависимый")
			.ДобавитьТест("ПараметрЗапускаЧитаетПортИзJSON")
			.ДобавитьТест("ПараметрЗапускаJSONБезПортаИспользуетПорт8080")
			.ДобавитьТест("ПараметрЗапускаMcpPortПерекрываетJSON")
			.ДобавитьТест("ПараметрЗапускаСОтсутствующимJSONВызываетИсключение")
			.ДобавитьТест("ПараметрЗапускаСНекорректнымJSONВызываетИсключение")
			.ДобавитьТест("ПараметрЗапускаСНекорректнымПортомJSONВызываетИсключение")
			.ДобавитьТест("ПараметрЗапускаСНекорректнымMcpPortВызываетИсключение")
			.ДобавитьТест("РегистрацияСохраняетИсточникРегистрации")
			.ДобавитьТест("РегистрацияИнструментаПриЗапущенномСервереСинхронизируетКомпоненту")
			.ДобавитьТест("РегистрацияИнструментаВоВремяСинхронизацииСохраняетПовторныйЗапрос")
		.ДобавитьТест("РезультатыТекстФормируетКонтент")
		.ДобавитьТест("РезультатыJSONСериализуетДанные")
		.ДобавитьТест("РезультатыРесурсФормируетContents")
		.ДобавитьТест("РезультатыРесурсJSONMimeСПараметрамиСериализуетJSON")
		.ДобавитьТест("РезультатыРесурсСохраняетСтрокуДляНеJSON")
		.ДобавитьТест("РезультатыПромптВозвращаетMessages")
		.ДобавитьТест("РезультатыТекстовыйПромптВозвращаетContentКакОбъект")
		.ДобавитьТест("СхемаПараметровСоздаетСхемуСEnum")
		.ДобавитьТест("ОшибкиВыбрасываютИсключения")
		.ДобавитьТест("ПодготовитьЗапросИнструментаНормализуетTaskРежим")
		.ДобавитьТест("ПодготовитьЗапросИнструментаСНедопустимымРежимомВозвращаетНеопределено")
		.ДобавитьТест("ПодготовитьЗапросИнструментаВTaskРежимеБезTaskIdВозвращаетНеопределено")
		.ДобавитьТест("ПодготовитьЗапросИнструментаВSyncРежимеБезIdВозвращаетНеопределено")
		.ДобавитьТест("ПодготовитьЗапросИнструментаБезArgumentsВозвращаетПустуюСтруктуру")
		.ДобавитьТест("ПодготовитьЗапросИнструментаВTaskРежимеСНекорректнымиАргументамиВызываетИсключение")
		.ДобавитьТест("ПодготовитьЗапросИнструментаЧитаетProgressTokenИзMeta")
		.ДобавитьТест("ДолгаяОперацияБольше30СекундОстаетсяОтложенной")
		.ДобавитьТест("ПодготовитьЗапросИнструментаВозвращаетЕдинуюМодель")
		.ДобавитьТест("РезультатДляЗадачиИзОтветаИнструментаДляResultВозвращаетResult")
		.ДобавитьТест("РезультатДляЗадачиИзОтветаИнструментаДляErrorВозвращаетErrorСтруктуру")
		.ДобавитьТест("РезультатДляЗадачиИзОтветаИнструментаДляНекорректногоEnvelopeВозвращаетНеопределено")
		.ДобавитьТест("ПубличнаяОтменаРегистрацииУдаляетПоИсточнику")
		.ДобавитьТест("FacadeЗадачОбрабатываетОтменуИОчистку")
			;
КонецПроцедуры

// Проверяет, что НовыйИнструмент заполняет режим выполнения.
	Процедура НовыйИнструментЗаполняетРежимВыполнения() Экспорт
		Оповещение = Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект);
		Схема = Новый Структура("type", "object");

	Описание = Мсп_Сервер.НовыйИнструмент("мой_инструмент_sync", "Описание", Схема, Оповещение);

	ЮТест.ОжидаетЧто(Описание.РежимВыполнения).Равно("sync");
	ЮТест.ОжидаетЧто(Описание.Включён).Равно(Истина);
	ЮТест.ОжидаетЧто(Описание.ОтложенныйОтвет).Равно(Ложь);
	ЮТест.ОжидаетЧто(Описание.ИсточникРегистрации).Равно("");
	КонецПроцедуры

	Процедура НовыйИнструментСОтложеннымОтветомЗаполняетПоля() Экспорт
		Оповещение = Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект);
		Схема = Новый Структура("type", "object");

		Описание = Мсп_Сервер.НовыйИнструмент(
			"мой_инструмент_deferred",
			"Описание",
			Схема,
			Оповещение,
			Истина);

	ЮТест.ОжидаетЧто(Описание.РежимВыполнения).Равно("sync");
	ЮТест.ОжидаетЧто(Описание.ОтложенныйОтвет).Равно(Истина);
КонецПроцедуры

	// Проверяет, что конструктор не принимает некорректный признак отложенного ответа.
	Процедура НовыйИнструментСНекорректнымОтложеннымОтветомВызываетИсключение() Экспорт
	Оповещение = Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект);
	СообщениеОшибки = "";

	Попытка
			Мсп_Сервер.НовыйИнструмент(
				"мой_инструмент_invalid",
				"Описание",
				Новый Структура("type", "object"),
				Оповещение,
				1);
	Исключение
		СообщениеОшибки = ОписаниеОшибки();
	КонецПопытки;

		ЮТест.ОжидаетЧто(СтрНайти(СообщениеОшибки, "ОтложенныйОтвет должен быть булевым.") > 0).ЭтоИстина();
	КонецПроцедуры

// Проверяет, что НовыйРесурс принимает явный mime type.
Процедура НовыйРесурсЗаполняетMimeType() Экспорт
	Оповещение = Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект);
	Описание = Мсп_Сервер.НовыйРесурс("test://resource/1", "Ресурс", "Описание", "text/plain", Оповещение);

	ЮТест.ОжидаетЧто(Описание.MimeType).Равно("text/plain");
	ЮТест.ОжидаетЧто(Описание.Провайдер).Равно("");
	ЮТест.ОжидаетЧто(Описание.Включён).Равно(Истина);
	ЮТест.ОжидаетЧто(Описание.ИсточникРегистрации).Равно("");
КонецПроцедуры

// Проверяет, что НовыйШаблонРесурса принимает явный mime type.
Процедура НовыйШаблонРесурсаЗаполняетMimeType() Экспорт
	Оповещение = Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект);
	Описание = Мсп_Сервер.НовыйШаблонРесурса("test://resource/{id}", "Шаблон", "Описание", "text/plain", Оповещение);

	ЮТест.ОжидаетЧто(Описание.MimeType).Равно("text/plain");
	ЮТест.ОжидаетЧто(Описание.Провайдер).Равно("");
	ЮТест.ОжидаетЧто(Описание.Включён).Равно(Истина);
	ЮТест.ОжидаетЧто(Описание.ИсточникРегистрации).Равно("");
КонецПроцедуры

// Проверяет конструктор промпта.
Процедура НовыйПромптЗаполняетПоля() Экспорт
	Оповещение = Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект);
	Аргументы = Новый Массив;
	Аргументы.Добавить(Новый Структура("name,description,required", "topic", "Тема", Истина));
	Описание = Мсп_Сервер.НовыйПромпт("summarize", "Описание промпта", Аргументы, Оповещение);

	ЮТест.ОжидаетЧто(Описание)
		.Свойство("Имя").Равно("summarize")
		.Свойство("Описание").Равно("Описание промпта")
		.Свойство("Аргументы").Равно(Аргументы)
		.Свойство("Обработчик").Равно(Оповещение);
	ЮТест.ОжидаетЧто(Описание.Провайдер).Равно("");
	ЮТест.ОжидаетЧто(Описание.Включён).Равно(Истина);
	ЮТест.ОжидаетЧто(Описание.ИсточникРегистрации).Равно("");
КонецПроцедуры

// Проверяет, что конструктор промпта принимает только массив аргументов.
Процедура НовыйПромптСНекорректнымиАргументамиВызываетИсключение() Экспорт
	Оповещение = Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект);
	СообщениеОшибки = "";

	Попытка
		Мсп_Сервер.НовыйПромпт("summarize", "Описание промпта", Новый Структура, Оповещение);
	Исключение
		СообщениеОшибки = ОписаниеОшибки();
	КонецПопытки;

	ЮТест.ОжидаетЧто(СтрНайти(СообщениеОшибки, "Аргументы промпта должны быть массивом.") > 0).ЭтоИстина();
КонецПроцедуры

// Проверяет конструкторы параметров и информации сервера.
Процедура ПараметрыСервераФормируютКонтракт() Экспорт
	ИнформацияОСервере = Мсп_Сервер.НоваяИнформацияОСервере("demo", "1.0.0", "Demo", "Описание", "Инструкции");
	Параметры = Мсп_Сервер.НовыйПараметрыСервера(7777, "*", 45, ИнформацияОСервере);

	ЮТест.ОжидаетЧто(ИнформацияОСервере)
		.Свойство("name").Равно("demo")
		.Свойство("version").Равно("1.0.0")
		.Свойство("title").Равно("Demo")
		.Свойство("description").Равно("Описание")
		.Свойство("instructions").Равно("Инструкции");
	ЮТест.ОжидаетЧто(Параметры)
		.Свойство("Порт").Равно(7777)
		.Свойство("РазрешенныеOrigins").Равно("*")
		.Свойство("ТаймаутСек").Равно(45)
		.Свойство("ИнформацияОСервере").Равно(ИнформацияОСервере);
КонецПроцедуры

// Проверяет зафиксированный таймаут ожидания ответа от обработчиков MCP.
Процедура ПараметрыСервераПоУмолчаниюФиксируютТаймаут900Секунд() Экспорт
	Параметры = Мсп_Сервер.НовыйПараметрыСервера(7777);

	ЮТест.ОжидаетЧто(Параметры.ТаймаутСек).Равно(900);
КонецПроцедуры

Процедура ПараметрЗапускаБезКомандыНеФормируетПараметрыСервера() Экспорт
	Параметры = Мсп_ПараметрыЗапускаКлиент.ПодготовитьПараметрыСервера("other=1");

	ЮТест.ОжидаетЧто(Параметры).ЭтоНеопределено();
КонецПроцедуры

Процедура ПараметрЗапускаRunMcpИспользуетПорт8080() Экспорт
	Параметры = Мсп_ПараметрыЗапускаКлиент.ПодготовитьПараметрыСервера("runMcp");

	ЮТест.ОжидаетЧто(Параметры.Порт).Равно(8080);
КонецПроцедуры

Процедура ПараметрЗапускаRunMcpРегистронезависимый() Экспорт
	Параметры = Мсп_ПараметрыЗапускаКлиент.ПодготовитьПараметрыСервера("RUNMCP;mcpPort=8090");

	ЮТест.ОжидаетЧто(Параметры.Порт).Равно(8090);
КонецПроцедуры

Процедура ПараметрЗапускаЧитаетПортИзJSON() Экспорт
	ПутьКонфигурации = ЮТТестовыеДанные.НовыйФайл("{""port"":8091}", , "json");
	Параметры = Мсп_ПараметрыЗапускаКлиент.ПодготовитьПараметрыСервера("runMcp=" + ПутьКонфигурации);

	ЮТест.ОжидаетЧто(Параметры.Порт).Равно(8091);
КонецПроцедуры

Процедура ПараметрЗапускаJSONБезПортаИспользуетПорт8080() Экспорт
	ПутьКонфигурации = ЮТТестовыеДанные.НовыйФайл("{""name"":""demo""}", , "json");
	Параметры = Мсп_ПараметрыЗапускаКлиент.ПодготовитьПараметрыСервера("runMcp=" + ПутьКонфигурации);

	ЮТест.ОжидаетЧто(Параметры.Порт).Равно(8080);
КонецПроцедуры

Процедура ПараметрЗапускаMcpPortПерекрываетJSON() Экспорт
	ПутьКонфигурации = ЮТТестовыеДанные.НовыйФайл("{""port"":8092}", , "json");
	Параметры = Мсп_ПараметрыЗапускаКлиент.ПодготовитьПараметрыСервера("runMcp=" + ПутьКонфигурации + ";mcpPort=8093");

	ЮТест.ОжидаетЧто(Параметры.Порт).Равно(8093);
КонецПроцедуры

Процедура ПараметрЗапускаСОтсутствующимJSONВызываетИсключение() Экспорт
	СообщениеОшибки = ОшибкаПодготовкиПараметров("runMcp=/tmp/mcp-missing-config.json");

	ЮТест.ОжидаетЧто(СтрНайти(СообщениеОшибки, "Файл конфигурации MCP не найден") > 0).ЭтоИстина();
КонецПроцедуры

Процедура ПараметрЗапускаСНекорректнымJSONВызываетИсключение() Экспорт
	ПутьКонфигурации = ЮТТестовыеДанные.НовыйФайл("не json", , "json");
	СообщениеОшибки = ОшибкаПодготовкиПараметров("runMcp=" + ПутьКонфигурации);

	ЮТест.ОжидаетЧто(СтрНайти(СообщениеОшибки, "Конфигурация MCP должна быть корректным JSON.") > 0).ЭтоИстина();
КонецПроцедуры

Процедура ПараметрЗапускаСНекорректнымПортомJSONВызываетИсключение() Экспорт
	ПутьКонфигурации = ЮТТестовыеДанные.НовыйФайл("{""port"":""bad""}", , "json");
	СообщениеОшибки = ОшибкаПодготовкиПараметров("runMcp=" + ПутьКонфигурации);

	ЮТест.ОжидаетЧто(СтрНайти(СообщениеОшибки, "Параметр port должен быть числом больше нуля.") > 0).ЭтоИстина();
КонецПроцедуры

Процедура ПараметрЗапускаСНекорректнымMcpPortВызываетИсключение() Экспорт
	СообщениеОшибки = ОшибкаПодготовкиПараметров("runMcp;mcpPort=bad");

	ЮТест.ОжидаетЧто(СтрНайти(СообщениеОшибки, "Параметр mcpPort должен быть числом больше нуля.") > 0).ЭтоИстина();
КонецПроцедуры

// Проверяет регистрацию описаний с явным источником.
Процедура РегистрацияСохраняетИсточникРегистрации() Экспорт
	Мсп_РеестрКлиент.ИнициализироватьРеестры();

	Оповещение = Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект);
	Источник = "ОМ_Сервер";
	Инструмент = Мсп_Сервер.НовыйИнструмент("tool-contract", "Описание", Новый Структура("type", "object"), Оповещение);
	Ресурс = Мсп_Сервер.НовыйРесурс("test://resource/contract", "Ресурс", "Описание", "application/json", Оповещение);
	ШаблонРесурса = Мсп_Сервер.НовыйШаблонРесурса("test://resource/{id}/contract", "Шаблон", "Описание", "application/json", Оповещение);
	Промпт = Мсп_Сервер.НовыйПромпт("prompt-contract", "Описание", Новый Массив, Оповещение);

	Мсп_Сервер.ЗарегистрироватьИнструмент(Инструмент, Источник);
	Мсп_Сервер.ЗарегистрироватьРесурс(Ресурс, Источник);
	Мсп_Сервер.ЗарегистрироватьШаблонРесурса(ШаблонРесурса, Источник);
	Мсп_Сервер.ЗарегистрироватьПромпт(Промпт, Источник);

	ИнструментВРеестре = Мсп_РеестрКлиент.РеестрИнструментов().Получить("tool-contract");
	РесурсВРеестре = Мсп_РеестрКлиент.РеестрРесурсов().Получить("test://resource/contract");
	ШаблонВРеестре = Мсп_РеестрКлиент.РеестрШаблоновРесурсов().Получить("test://resource/{id}/contract");
	ПромптВРеестре = Мсп_РеестрКлиент.РеестрПромптов().Получить("prompt-contract");

	ЮТест.ОжидаетЧто(ИнструментВРеестре)
		.Свойство("ИсточникРегистрации").Равно(Источник)
		.Свойство("Включён").Равно(Истина);
	ЮТест.ОжидаетЧто(РесурсВРеестре)
		.Свойство("ИсточникРегистрации").Равно(Источник)
		.Свойство("Провайдер").Равно("")
		.Свойство("Включён").Равно(Истина);
	ЮТест.ОжидаетЧто(ШаблонВРеестре)
		.Свойство("ИсточникРегистрации").Равно(Источник)
		.Свойство("Провайдер").Равно("")
		.Свойство("Включён").Равно(Истина);
	ЮТест.ОжидаетЧто(ПромптВРеестре)
		.Свойство("ИсточникРегистрации").Равно(Источник)
		.Свойство("Провайдер").Равно("")
		.Свойство("Включён").Равно(Истина);

	ЮТест.ОжидаетЧто(Мсп_РеестрКлиент.РеестрИнструментов(Истина).Получить("tool-contract")).НеРавно(Неопределено);
	ЮТест.ОжидаетЧто(Мсп_РеестрКлиент.РеестрРесурсов(Истина).Получить("test://resource/contract")).НеРавно(Неопределено);
	ЮТест.ОжидаетЧто(Мсп_РеестрКлиент.РеестрШаблоновРесурсов(Истина).Получить("test://resource/{id}/contract")).НеРавно(Неопределено);
	ЮТест.ОжидаетЧто(Мсп_РеестрКлиент.РеестрПромптов(Истина).Получить("prompt-contract")).НеРавно(Неопределено);
КонецПроцедуры

Асинх Процедура РегистрацияИнструментаПриЗапущенномСервереСинхронизируетКомпоненту() Экспорт
	Мсп_РеестрКлиент.ИнициализироватьРеестры();
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("СерверЗапущен", Истина);
	ТестКомпонентаMCP.Инициализировать();
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("Компонента", ТестКомпонентаMCP);
	ОчиститьФлагиСинхронизации();

	Инструмент = Мсп_Сервер.НовыйИнструмент(
		"tool-dynamic",
		"Описание",
		Новый Структура("type", "object"),
		Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект));

	Мсп_Сервер.ЗарегистрироватьИнструмент(Инструмент, "ОМ_Сервер");
	Ждать Мсп_ТранспортРеестрыКлиент.ВыполнитьЗапланированныеСинхронизации();

	ЮТест.ОжидаетЧто(
		Мсп_СостояниеКлиент.ЗначениеСостоянияСервера("ТребуетсяСинхронизацияИнструментов", Ложь))
		.ЭтоЛожь();
	ЮТест.ОжидаетЧто(
		Мсп_СостояниеКлиент.ЗначениеСостоянияСервера("ВыполняетсяСинхронизацияРеестров", Ложь))
		.ЭтоЛожь();
	ЮТест.ОжидаетЧто(ТестКомпонентаMCP.Инструменты().Найти("tool-dynamic")).НеРавно(Неопределено);
	ЮТест.ОжидаетЧто(ТестКомпонентаMCP.УведомленияИнструментов()).Равно(1);

	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("СерверЗапущен", Ложь);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("Компонента", Неопределено);
	ОчиститьФлагиСинхронизации();
КонецПроцедуры

Асинх Процедура РегистрацияИнструментаВоВремяСинхронизацииСохраняетПовторныйЗапрос() Экспорт
	Мсп_РеестрКлиент.ИнициализироватьРеестры();
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("СерверЗапущен", Истина);
	ТестКомпонентаMCP.Инициализировать();
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("Компонента", ТестКомпонентаMCP);
	ОчиститьФлагиСинхронизации();
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ВыполняетсяСинхронизацияРеестров", Истина);

	Инструмент = Мсп_Сервер.НовыйИнструмент(
		"tool-queued-dynamic",
		"Описание",
		Новый Структура("type", "object"),
		Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект));

	Мсп_Сервер.ЗарегистрироватьИнструмент(Инструмент, "ОМ_Сервер");

	ЮТест.ОжидаетЧто(
		Мсп_СостояниеКлиент.ЗначениеСостоянияСервера("ТребуетсяСинхронизацияИнструментов", Ложь))
		.ЭтоИстина();
	ЮТест.ОжидаетЧто(ТестКомпонентаMCP.Инструменты().Найти("tool-queued-dynamic")).Равно(Неопределено);

	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ВыполняетсяСинхронизацияРеестров", Ложь);
	Мсп_ТранспортРеестрыКлиент.ЗапланироватьСинхронизациюИнструментов();
	Ждать Мсп_ТранспортРеестрыКлиент.ВыполнитьЗапланированныеСинхронизации();

	ЮТест.ОжидаетЧто(
		Мсп_СостояниеКлиент.ЗначениеСостоянияСервера("ТребуетсяСинхронизацияИнструментов", Ложь))
		.ЭтоЛожь();
	ЮТест.ОжидаетЧто(
		Мсп_СостояниеКлиент.ЗначениеСостоянияСервера("ВыполняетсяСинхронизацияРеестров", Ложь))
		.ЭтоЛожь();
	ЮТест.ОжидаетЧто(ТестКомпонентаMCP.Инструменты().Найти("tool-queued-dynamic")).НеРавно(Неопределено);

	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("СерверЗапущен", Ложь);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("Компонента", Неопределено);
	ОчиститьФлагиСинхронизации();
КонецПроцедуры

// Проверяет конструктор текстового результата.
Процедура РезультатыТекстФормируетКонтент() Экспорт
	Результат = Мсп_Результаты.Текст("hello");

	ЮТест.ОжидаетЧто(Результат.content[0].type).Равно("text");
	ЮТест.ОжидаетЧто(Результат.content[0].text).Равно("hello");
КонецПроцедуры

// Проверяет сериализацию данных в JSON-результате.
Процедура РезультатыJSONСериализуетДанные() Экспорт
	Данные = Новый Структура("name,count", "demo", 2);
	Результат = Мсп_Результаты.JSON(Данные);

	ЮТест.ОжидаетЧто(Результат.content[0].type).Равно("text");
	ЮТест.ОжидаетЧто(Результат.content[0].text).Равно(Мсп_СериализацияКлиент.Сериализовать(Данные));
КонецПроцедуры

// Проверяет конструктор результата чтения ресурса.
Процедура РезультатыРесурсФормируетContents() Экспорт
	Данные = Новый Структура("id,name", 1, "resource");
	Результат = Мсп_Результаты.Ресурс("test://resource/1", Данные);

	ЮТест.ОжидаетЧто(Результат.contents[0].uri).Равно("test://resource/1");
	ЮТест.ОжидаетЧто(Результат.contents[0].mimeType).Равно("application/json");
	ЮТест.ОжидаетЧто(Результат.contents[0].text).Равно(Мсп_СериализацияКлиент.Сериализовать(Данные));
КонецПроцедуры

// Проверяет JSON-сериализацию для mime type application/json с параметрами.
Процедура РезультатыРесурсJSONMimeСПараметрамиСериализуетJSON() Экспорт
	Данные = Новый Структура("id,name", 2, "resource-2");
	Результат = Мсп_Результаты.Ресурс("test://resource/2", Данные, "application/json; charset=utf-8");

	ЮТест.ОжидаетЧто(Результат.contents[0].mimeType).Равно("application/json; charset=utf-8");
	ЮТест.ОжидаетЧто(Результат.contents[0].text).Равно(Мсп_СериализацияКлиент.Сериализовать(Данные));
КонецПроцедуры

// Проверяет, что не-JSON ресурс не оборачивает строку в JSON-кавычки.
Процедура РезультатыРесурсСохраняетСтрокуДляНеJSON() Экспорт
	Результат = Мсп_Результаты.Ресурс("test://resource/raw", "raw text", "text/plain");

	ЮТест.ОжидаетЧто(Результат.contents[0].mimeType).Равно("text/plain");
	ЮТест.ОжидаетЧто(Результат.contents[0].text).Равно("raw text");
КонецПроцедуры

// Проверяет конструктор результата промпта.
Процедура РезультатыПромптВозвращаетMessages() Экспорт
	Контент = Новый Структура("type,text", "text", "hello");
	Сообщение = Новый Структура("role,content", "user", Контент);
	Сообщения = Новый Массив;
	Сообщения.Добавить(Сообщение);

	Результат = Мсп_Результаты.Промпт(Сообщения);

	ЮТест.ОжидаетЧто(Результат.messages).Равно(Сообщения);
КонецПроцедуры

// Проверяет helper текстового prompt в MCP-совместимом формате.
Процедура РезультатыТекстовыйПромптВозвращаетContentКакОбъект() Экспорт
	Результат = Мсп_Результаты.ТекстовыйПромпт("hello");

	ЮТест.ОжидаетЧто(Результат.messages.Количество()).Равно(1);
	ЮТест.ОжидаетЧто(Результат.messages[0].role).Равно("user");
	ЮТест.ОжидаетЧто(Результат.messages[0].content.type).Равно("text");
	ЮТест.ОжидаетЧто(Результат.messages[0].content.text).Равно("hello");
КонецПроцедуры

// Проверяет конструкторы схемы параметров с enum-полем.
Процедура СхемаПараметровСоздаетСхемуСEnum() Экспорт
	ДопустимыеЗначения = Новый Массив;
	ДопустимыеЗначения.Добавить("sync");
	ДопустимыеЗначения.Добавить("task");
	Свойство = Мсп_СхемаПараметров.НовыйСвойствоСхемыСEnum("string", "Режим", ДопустимыеЗначения);

	Свойства = Новый Структура;
	Свойства.Вставить("executionMode", Свойство);
	ТребуемыеПоля = Новый Массив;
	ТребуемыеПоля.Добавить("executionMode");
	Схема = Мсп_СхемаПараметров.НовыйСхемаОбъекта(Свойства, ТребуемыеПоля);

	ЮТест.ОжидаетЧто(Схема.type).Равно("object");
	ЮТест.ОжидаетЧто(Схема.properties.executionMode.enum).Равно(ДопустимыеЗначения);
	ЮТест.ОжидаетЧто(Схема.required).Равно(ТребуемыеПоля);
КонецПроцедуры

// Проверяет доступность имен в модуле ошибок.
Процедура ОшибкиВыбрасываютИсключения() Экспорт
	ПроверитьКодОшибки("НекорректныеПараметры", "INVALID_PARAMS");
	ПроверитьКодОшибки("НеНайдено", "NOT_FOUND");
	ПроверитьКодОшибки("ДоступЗапрещен", "ACTION_DENIED");
	ПроверитьКодОшибки("СбойВыполнения", "ACTION_FAILED");
	ПроверитьКодОшибки("Внутренняя", "ACTION_FAILED");
КонецПроцедуры

// Проверяет нормализацию входного MCP_TOOL_CALL для task-сценария.
Процедура ПодготовитьЗапросИнструментаНормализуетTaskРежим() Экспорт
	ЗапросJSON = "{""name"":""Timer"",""arguments"":{""ticks"":3},""executionMode"":""task"",""taskId"":""task-123"",""progressToken"":""progress-123""}";

	ВызовИнструмента = Мсп_ТранспортСобытияКлиент.ПодготовитьЗапросИнструмента(ЗапросJSON);

	ЮТест.ОжидаетЧто(ВызовИнструмента)
		.Свойство("ИмяИнструмента").Равно("Timer")
		.Свойство("РежимВыполнения").Равно("task")
		.Свойство("ИдентификаторЗадачи").Равно("task-123")
		.Свойство("ProgressToken").Равно("progress-123");
	ЮТест.ОжидаетЧто(ВызовИнструмента.Аргументы.ticks).Равно(3);
КонецПроцедуры

// Проверяет отказ transport-нормализации для недопустимого executionMode.
Процедура ПодготовитьЗапросИнструментаСНедопустимымРежимомВозвращаетНеопределено() Экспорт
	ЗапросJSON = "{""name"":""Timer"",""arguments"":{""ticks"":3},""executionMode"":""async"",""id"":""request-1""}";

	Контекст = Мсп_ТранспортСобытияКлиент.ПодготовитьЗапросИнструмента(ЗапросJSON);

	ЮТест.ОжидаетЧто(Контекст).ЭтоНеопределено();
КонецПроцедуры

// Проверяет, что transport не пропускает task-вызов без taskId.
Процедура ПодготовитьЗапросИнструментаВTaskРежимеБезTaskIdВозвращаетНеопределено() Экспорт
	ЗапросJSON = "{""name"":""Timer"",""arguments"":{""ticks"":3},""executionMode"":""task""}";

	ВызовИнструмента = Мсп_ТранспортСобытияКлиент.ПодготовитьЗапросИнструмента(ЗапросJSON);

	ЮТест.ОжидаетЧто(ВызовИнструмента).ЭтоНеопределено();
КонецПроцедуры

// Проверяет, что transport не пропускает sync-вызов без id.
Процедура ПодготовитьЗапросИнструментаВSyncРежимеБезIdВозвращаетНеопределено() Экспорт
	ЗапросJSON = "{""name"":""Timer"",""arguments"":{""ticks"":3}}";

	ВызовИнструмента = Мсп_ТранспортСобытияКлиент.ПодготовитьЗапросИнструмента(ЗапросJSON);

	ЮТест.ОжидаетЧто(ВызовИнструмента).ЭтоНеопределено();
КонецПроцедуры

// Проверяет, что отсутствие arguments обрабатывается на границе сборки запроса.
Процедура ПодготовитьЗапросИнструментаБезArgumentsВозвращаетПустуюСтруктуру() Экспорт
	ЗапросJSON = "{""name"":""Timer"",""id"":""request-1""}";

	ВызовИнструмента = Мсп_ТранспортСобытияКлиент.ПодготовитьЗапросИнструмента(ЗапросJSON);

	ЮТест.ОжидаетЧто(ВызовИнструмента).НеРавно(Неопределено);
	ЮТест.ОжидаетЧто(ТипЗнч(ВызовИнструмента.Аргументы)).Равно(Тип("Структура"));
	ЮТест.ОжидаетЧто(ВызовИнструмента.Аргументы.Количество()).Равно(0);
КонецПроцедуры

// Проверяет strict-контракт arguments для task вызова инструмента.
Процедура ПодготовитьЗапросИнструментаВTaskРежимеСНекорректнымиАргументамиВызываетИсключение() Экспорт
	ЗапросJSON = "{""name"":""Timer"",""executionMode"":""task"",""taskId"":""task-123"",""arguments"":""bad""}";
	ТекстОшибки = "";

	Попытка
		Мсп_ТранспортСобытияКлиент.ПодготовитьЗапросИнструмента(ЗапросJSON);
	Исключение
		ТекстОшибки = ОписаниеОшибки();
	КонецПопытки;

	ЮТест.ОжидаетЧто(ТекстОшибки <> "").ЭтоИстина();
КонецПроцедуры

// Проверяет чтение progressToken из _meta при нормализации MCP_TOOL_CALL.
Процедура ПодготовитьЗапросИнструментаЧитаетProgressTokenИзMeta() Экспорт
	ЗапросJSON = "{""name"":""Timer"",""arguments"":{""ticks"":3},""_meta"":{""progressToken"":""progress-meta""},""progressToken"":""progress-root"",""id"":""request-1""}";

	ВызовИнструмента = Мсп_ТранспортСобытияКлиент.ПодготовитьЗапросИнструмента(ЗапросJSON);

	ЮТест.ОжидаетЧто(ВызовИнструмента)
		.Свойство("Идентификатор").Равно("request-1")
		.Свойство("ProgressToken").Равно("progress-meta");
КонецПроцедуры

// Проверяет, что долгая операция больше штатного таймаута остается отложенной.
Процедура ДолгаяОперацияБольше30СекундОстаетсяОтложенной() Экспорт
	Мсп_РеестрКлиент.ИнициализироватьРеестры();
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ПоследнийЗапросДолгойОперации", Неопределено);
	Оповещение = Новый ОписаниеОповещения("ОбработчикДолгойОперации", ЭтотОбъект);
	Инструмент = Мсп_Сервер.НовыйИнструмент(
		"long-operation-31",
		"Long operation",
		Новый Структура("type", "object"),
		Оповещение,
		Истина);
	Мсп_Сервер.ЗарегистрироватьИнструмент(Инструмент, "ОМ_Сервер");

	ВызовИнструмента = Мсп_ПротоколКлиент.НовыйВызовИнструмента(
		"request-long-31",
		"long-operation-31",
		Новый Структура("durationSeconds", 31),
		"",
		"sync",
		"",
		"progress-long-31");

	НачалоВызова = ТекущаяУниверсальнаяДатаВМиллисекундах();
	Ответ = Мсп_ПротоколКлиент.ВызватьИнструмент(ВызовИнструмента);
	ДлительностьВызова = ТекущаяУниверсальнаяДатаВМиллисекундах() - НачалоВызова;
	ПоследнийЗапрос = Мсп_СостояниеКлиент.ЗначениеСостоянияСервера("ПоследнийЗапросДолгойОперации");

	ЮТест.ОжидаетЧто(Ответ).ЭтоНеопределено();
	ЮТест.ОжидаетЧто(ДлительностьВызова >= 30000).ЭтоИстина();
	ЮТест.ОжидаетЧто(ПоследнийЗапрос)
		.Свойство("Идентификатор").Равно("request-long-31");
	ЮТест.ОжидаетЧто(ПоследнийЗапрос.Аргументы.durationSeconds).Равно(31);
	ЮТест.ОжидаетЧто(ПоследнийЗапрос.Контекст.progressToken).Равно("progress-long-31");
КонецПроцедуры

// Проверяет, что transport сразу возвращает единый контракт вызова инструмента.
Процедура ПодготовитьЗапросИнструментаВозвращаетЕдинуюМодель() Экспорт
	ЗапросJSON = "{""name"":""Timer"",""arguments"":{""ticks"":3},""id"":""request-1""}";

	ВызовИнструмента = Мсп_ТранспортСобытияКлиент.ПодготовитьЗапросИнструмента(ЗапросJSON);

	ЮТест.ОжидаетЧто(ВызовИнструмента)
		.Свойство("Идентификатор").Равно("request-1")
		.Свойство("ИмяИнструмента").Равно("Timer")
		.Свойство("Сессия").Равно("")
		.Свойство("РежимВыполнения").Равно("sync")
		.Свойство("ИдентификаторЗадачи").Равно("");
	ЮТест.ОжидаетЧто(ВызовИнструмента.ProgressToken).Равно(Неопределено);
	ЮТест.ОжидаетЧто(ВызовИнструмента.Аргументы.ticks).Равно(3);
КонецПроцедуры

Процедура РезультатДляЗадачиИзОтветаИнструментаДляResultВозвращаетResult() Экспорт
	Результат = Новый Структура("content", Новый Массив);
	Ответ = Новый Структура("result", Результат);

	Преобразовано = Мсп_ТранспортСобытияКлиент.РезультатДляЗадачиИзОтветаИнструмента(Ответ);

	ЮТест.ОжидаетЧто(Преобразовано).Равно(Результат);
КонецПроцедуры

Процедура РезультатДляЗадачиИзОтветаИнструментаДляErrorВозвращаетErrorСтруктуру() Экспорт
	Ошибка = Новый Структура("code,message", -32603, "Tool result is invalid");
	Ответ = Новый Структура("error", Ошибка);

	Преобразовано = Мсп_ТранспортСобытияКлиент.РезультатДляЗадачиИзОтветаИнструмента(Ответ);

	ЮТест.ОжидаетЧто(Преобразовано).Равно(Новый Структура("error", Ошибка));
КонецПроцедуры

Процедура РезультатДляЗадачиИзОтветаИнструментаДляНекорректногоEnvelopeВозвращаетНеопределено() Экспорт
	Ответ = Новый Структура(
		"result,error",
		Новый Структура("content", Новый Массив),
		Новый Структура("code,message", -32603, "invalid"));

	Преобразовано = Мсп_ТранспортСобытияКлиент.РезультатДляЗадачиИзОтветаИнструмента(Ответ);

	ЮТест.ОжидаетЧто(Преобразовано).ЭтоНеопределено();
КонецПроцедуры

// Проверяет, что публичный wrapper ОтменитьРегистрацию делегирует удаление в реестр.
Процедура ПубличнаяОтменаРегистрацииУдаляетПоИсточнику() Экспорт
	Мсп_РеестрКлиент.ИнициализироватьРеестры();

	Оповещение = Новый ОписаниеОповещения("ОбработчикТест", ЭтотОбъект);
	Источник = "OM_SERVER_UNREGISTER";
	Инструмент = Мсп_Сервер.НовыйИнструмент("tool-wrapper-unregister", "Описание", Новый Структура("type", "object"), Оповещение);

	Мсп_Сервер.ЗарегистрироватьИнструмент(Инструмент, Источник);
	Мсп_Сервер.ОтменитьРегистрацию(Источник);

	ЮТест.ОжидаетЧто(Мсп_РеестрКлиент.РеестрИнструментов().Получить("tool-wrapper-unregister")).Равно(Неопределено);
КонецПроцедуры

// Проверяет, что facade задач использует единое хранилище отмены.
Процедура FacadeЗадачОбрабатываетОтменуИОчистку() Экспорт
	ИдентификаторЗадачи = "task-cancelled-1";
	Мсп_Сервер.ОчиститьОтменуЗадачи(ИдентификаторЗадачи);
	Мсп_ЗадачиКлиент.ОбработатьTaskCancelled("{""taskId"":""task-cancelled-1""}");

	ЮТест.ОжидаетЧто(Мсп_Сервер.ЗадачаОтменена(ИдентификаторЗадачи)).Равно(Истина);

	Мсп_Сервер.ОчиститьОтменуЗадачи(ИдентификаторЗадачи);
	ЮТест.ОжидаетЧто(Мсп_Сервер.ЗадачаОтменена(ИдентификаторЗадачи)).Равно(Ложь);
КонецПроцедуры

Функция ОшибкаПодготовкиПараметров(ПараметрЗапуска)
	СообщениеОшибки = "";

	Попытка
		Мсп_ПараметрыЗапускаКлиент.ПодготовитьПараметрыСервера(ПараметрЗапуска);
	Исключение
		СообщениеОшибки = ОписаниеОшибки();
	КонецПопытки;

	Возврат СообщениеОшибки;
КонецФункции

Процедура ОбработчикТест() Экспорт
	
КонецПроцедуры

Процедура ОчиститьФлагиСинхронизации()
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ТребуетсяСинхронизацияИнструментов", Ложь);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ТребуетсяСинхронизацияРесурсов", Ложь);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ТребуетсяСинхронизацияПромптов", Ложь);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ВыполняетсяСинхронизацияРеестров", Ложь);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ЗапланированОбработчикСинхронизацииРеестров", Ложь);
КонецПроцедуры

Функция ОбработчикДолгойОперации(Запрос, ДополнительныеПараметры) Экспорт
	ЮТест.Пауза(Запрос.Аргументы.durationSeconds);
	Мсп_СостояниеКлиент.УстановитьЗначениеСостоянияСервера("ПоследнийЗапросДолгойОперации", Запрос);
	Возврат Неопределено;
КонецФункции

Функция ВызватьИПолучитьТекстИсключения(ИмяМетодаОшибки)
	Попытка
		Если ИмяМетодаОшибки = "НекорректныеПараметры" Тогда
			Мсп_Ошибки.НекорректныеПараметры("test");
		ИначеЕсли ИмяМетодаОшибки = "НеНайдено" Тогда
			Мсп_Ошибки.НеНайдено("test");
		ИначеЕсли ИмяМетодаОшибки = "Внутренняя" Тогда
			Мсп_Ошибки.Внутренняя("test");
		ИначеЕсли ИмяМетодаОшибки = "ДоступЗапрещен" Тогда
			Мсп_Ошибки.ДоступЗапрещен("test");
		ИначеЕсли ИмяМетодаОшибки = "СбойВыполнения" Тогда
			Мсп_Ошибки.СбойВыполнения("test");
		КонецЕсли;
	Исключение
		Возврат ОписаниеОшибки();
	КонецПопытки;

	Возврат "";
КонецФункции

Процедура ПроверитьКодОшибки(ИмяМетодаОшибки, ОжидаемыйКодОшибки)
	КодОшибки = ВызватьИПолучитьКодОшибки(ИмяМетодаОшибки);
	ЮТест.ОжидаетЧто(КодОшибки).Равно(ОжидаемыйКодОшибки);
КонецПроцедуры

Функция ВызватьИПолучитьКодОшибки(ИмяМетодаОшибки)
	Попытка
		Если ИмяМетодаОшибки = "НекорректныеПараметры" Тогда
			Мсп_Ошибки.НекорректныеПараметры("test");
		ИначеЕсли ИмяМетодаОшибки = "НеНайдено" Тогда
			Мсп_Ошибки.НеНайдено("test");
		ИначеЕсли ИмяМетодаОшибки = "Внутренняя" Тогда
			Мсп_Ошибки.Внутренняя("test");
		ИначеЕсли ИмяМетодаОшибки = "ДоступЗапрещен" Тогда
			Мсп_Ошибки.ДоступЗапрещен("test");
		ИначеЕсли ИмяМетодаОшибки = "СбойВыполнения" Тогда
			Мсп_Ошибки.СбойВыполнения("test");
		КонецЕсли;
	Исключение
		Информация = ИнформацияОбОшибке();
		Возврат Информация.Код;
	КонецПопытки;

	Возврат "";
КонецФункции

#КонецОбласти
